translator:
  save-changes: حفظ
  add-language: إضافة لغة
  missing-translation: ترجمة مفقودة
resources:
  namespace:
    title: Namespace "{{ handle }}"
  module:
    title: Module "{{ handle }}"
    field:
      title: Module field "{{ name }}"
  page:
    title: Page "{{ handle }}"
    block:
      title: "Page block #{{ blockID }}"
