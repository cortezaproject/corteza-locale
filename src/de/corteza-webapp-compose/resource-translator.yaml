translator:
  save-changes: Speichern
  add-language: Sprache hinzufügen
  missing-translation: Fehlende Übersetzung
resources:
  namespace:
    title: Namensraum "{{ handle }}"
  module:
    title: Modul "{{ handle }}"
    field:
      title: Modul Feld "{{ name }}"
  page:
    title: Seite {{ handle }}
    block:
      title: "Seiten Block #{{ blockID }}"
